[project]
name = "autoresearch-mcp"
version = "0.1.0"
description = "Autonomous iterative improvement agent system with frozen evaluation"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
